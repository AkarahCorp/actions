name: actions
version: '1.0-SNAPSHOT'
main: dev.akarah.actions.Main
bootstrapper: dev.akarah.actions.Bootstrapper
api-version: '1.21'
prefix: actions
folia-supported: true

dependencies:
  bootstrap:
    plugin-packs:
      load: BEFORE
      required: true
      join-classpath: true
  server:
    plugin-packs:
      load: BEFORE
      required: true
      join-classpath: true
